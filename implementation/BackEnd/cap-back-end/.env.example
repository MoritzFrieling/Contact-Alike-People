# Copy this file to .env locally or define the variables in your shell.
# Never commit a .env file or real database credentials.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=cap_user
DB_PASSWORD=
DB_NAME=cap
